services:
  postgres:
    image: postgres:16
    environment: { POSTGRES_DB: domainscout, POSTGRES_USER: postgres, POSTGRES_PASSWORD: postgres }
    ports: ["5432:5432"]
    volumes: [postgres:/var/lib/postgresql/data]
  redis:
    image: redis:7
    ports: ["6379:6379"]
  web:
    build: .
    command: npm run dev
    env_file: .env
    ports: ["3000:3000"]
    depends_on: [postgres, redis]
volumes: { postgres: {} }
