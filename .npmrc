registry=https://registry.npmjs.org/
fund=false
audit=false
# If your shell exports http-proxy/https-proxy and public package installs fail with E403,
# unset those environment variables or point them at an authorized company proxy.
